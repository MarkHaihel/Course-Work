@page
@model LoginWithRecoveryCodeModel
@{
    ViewData["Title"] = "Перевірка коду відновлення";
}

<h2>@ViewData["Title"]</h2>
<hr />
<p>
    Ви подали запит на вхід за допомогою коду відновлення. Цей вхід не буде запам'ятовуватися, поки ви не надасте
    код програми аутентифікації при вході або вимкніть 2FA і знову увійдіть.
</p>
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Input.RecoveryCode">Код відновлення</label>
                <input asp-for="Input.RecoveryCode" class="form-control" autocomplete="off" />
                <span asp-validation-for="Input.RecoveryCode" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-outline-dark">Вхід</button>
        </form>
    </div>
</div>