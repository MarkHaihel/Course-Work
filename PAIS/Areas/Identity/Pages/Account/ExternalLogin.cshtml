@page
@model ExternalLoginModel
@{
    ViewData["Title"] = "Реєстрація";
}

<h2>@ViewData["Title"]</h2>
<h4>Приєднайте свій @Model.LoginProvider обліковий запис.</h4>
<hr />

<p class="text-info">
    Ви успішно автентифікувались за допомогою <strong>@Model.LoginProvider</strong>.
    Введіть адресу електронної пошти для цього сайту нижче та натисніть кнопку Реєстрація, щоб завершити вхід.
</p>

<div class="row">
    <div class="col-md-4">
        <form asp-page-handler="Confirmation" asp-route-returnUrl="@Model.ReturnUrl" method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Input.Email"></label>
                <input asp-for="Input.Email" class="form-control" />
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-outline-dark">Реєстрація</button>
        </form>
    </div>
</div>